# 2026-08-05 工作日志

## 三个安卓端功能任务(未提交 git)

### 任务1:全屏双指缩放(改进已有实现)
- `simple_live_app/lib/modules/live_room/player/player_controller.dart` `PlayerGestureControlMixin`:
  - 重写 `onScaleUpdate` 偏移计算为**焦点锚定**公式(以手势焦点为中心缩放,保持焦点下内容不动)
  - 增加**平移边界 clamp**(防画面拖出屏幕,范围 ±(scale-1)/2 × 屏宽高)
  - 缩放开始自动隐藏控制条;scale 回 1.0 时偏移归零;上限 5.0
- 已有渲染层(live_room_page.dart buildMediaPlayer,仅 fullScreenState 时挂 GestureDetector+Transform)未改动

### 任务2:抖音号搜索
- `simple_live_core/lib/src/model/live_room_item.dart`:新增 `uniqueId`(抖音号)、`secUid` 可选字段
- `simple_live_core/lib/src/douyin_site.dart`:
  - `searchRooms` 解析 owner 的 `unique_id`/`sec_uid`;抖音号精确匹配结果**置顶**;live search 空结果时 fallback `searchLiveRoomByDouyinId`
  - 新增 `searchLiveRoomByDouyinId`:discover/search(aweme_user)→ sec_uid → user/profile/other 查开播(status==2)→ 组装 LiveRoomItem;全程容错,失败静默返回空
  - 新增 `_getLiveRoomInfoBySecUid`:解析 `user.room_data.room` 拿 web_rid/room_id
- `simple_live_app/lib/modules/search/search_controller.dart`:`_tryOpenDouyinLink` 增加抖音号格式(字母开头)时 `_tryOpenByDouyinId` 直达直播间(失败静默,不拦截普通搜索)
- `simple_live_app/lib/widgets/live_room_card.dart`:有 uniqueId 时显示"抖音号: xxx"
- 注意:抖音接口风控严(discover/search 未登录返回空/需登录),链路为尽力而为,登录抖音账号(cookie)后成功率更高

### 任务3:录屏(全新实现)
- 依赖:`flutter_screen_recording: ^2.0.25`(MediaProjection + 前台服务,自动拉 flutter_foreground_task 9.x)
- `simple_live_app/lib/services/screen_recorder_service.dart`(新文件,GetxService 全局单例):
  - `start()`:Android 10+ 用 `startRecordScreenWithInternalAudio`(录直播内音),低版本 `startRecordScreen`(仅画面);申请通知权限(Android 13+);计时器记录时长
  - `stop()`:返回保存路径(Movies/ScreenRecordings,相册可见),Toast 提示
  - `isRecording`/`recordSeconds`/`recordTimeText` 供 UI 响应式显示
- 入口1:直播页"更多"菜单(仅 Android,截图项下方)—— 录制中显示"停止录屏(mm:ss)"红点
- 入口2:全屏控制条顶部(截图按钮旁)红色录制按钮(仅 Android)
- `main.dart` initServices 注册;`AndroidManifest.xml` 加 RECORD_AUDIO/FOREGROUND_SERVICE/FOREGROUND_SERVICE_MEDIA_PROJECTION/POST_NOTIFICATIONS/WAKE_LOCK;proguard 加 keep 规则(com.isvisoft.flutter_screen_recording / com.pravera.flutter_foreground_task)

## 其他
- 本机无 Flutter/Dart SDK，无法 pub get / analyze / build；用 Python tokenizer 做了括号平衡静态检查(全部通过)
- 修改均未 git 提交，待用户 review 后自行提交

## 下午:排查用户"打包打不上"(2026-08-05 13:21)

### 排查过程
- 用户已把三个任务的修改提交为 `7920a32 测试`(13 文件,522 行新增)
- app/tv_app 的 `android/build/reports/problems/problems-report.html` 均只有 "Gradle Configuration Cache / Loading..." → 构建卡在 **Gradle 配置阶段**
- **根因确认:simple_live_app 目录下无 pubspec.lock / .dart_tool / .flutter-plugins-dependencies → `flutter pub get` 从未成功执行** → Gradle 无法识别新插件,配置阶段失败

### 代码验证(排除代码问题)
- 下载独立 Dart SDK 3.10.0 到 `C:\Users\xxl\AppData\Local\Temp\dart_sdk`
- `dart format --output=none --set-exit-if-changed` 对 9 个修改文件:全部语法正确
- simple_live_core(纯 Dart)目录 `dart pub get` 成功(63 依赖,网络可访问 pub.dev)+ `dart analyze lib/src/douyin_site.dart lib/src/model/live_room_item.dart` → **No issues found!**
- 依赖链约束检查:flutter_screen_recording 2.0.25 → flutter_foreground_task ^9.1.0(需 Flutter>=3.22/Dart>=3.4,项目 3.38 满足)+ platform_interface/web 包无冲突
- 工具链:AGP 8.9.1 / Kotlin 2.1.0 / Gradle 8.12 均满足插件要求(>=8.6/1.9.10/8.6)

### 结论与修复指引(已告知用户)
- 代码没问题,问题在 pub get 未执行
- 修复:在 simple_live_app 目录执行 `flutter pub get`,成功后再打包;若 pub get 报错需贴错误信息
- 用户机器找不到 Flutter SDK 命令行(未装到 PATH,AS 也无 Flutter 插件配置),打包环境不明

## 下午 2:17:确认用户用 GitHub Actions 构建 + 找到真正编译错误

### 用户环境
- 构建走 `.github/workflows/publish_app_dev.yaml`(macos runner 打 APK + windows runner 打 Windows)
- Flutter 3.38.x,`flutter pub get` → `flutter build apk --release --split-per-abi`

### 真正根因(编译错误)
- **flutter_screen_recording 2.0.25(pub.dev 正式版)不存在 `startRecordScreenWithInternalAudio` 方法!**
- 此前 WebFetch 的是 putpin fork(master 分支)的 API,与 pub.dev 2.0.25 不一致
- 我的 `screen_recorder_service.dart` 调用了不存在的方法 → `flutter build apk` Dart 编译失败 → CI 打包失败
- 已下载 2.0.25 源码包确认:
  - 只有 `startRecordScreen`(无声)/ `startRecordScreenAndAudio`(麦克风音)两个方法
  - Android 音频源是 `MediaRecorder.AudioSource.MIC`,无内音录制
  - `stopRecordScreen` 调 `FlutterForegroundTask.stopService()`,因 fft service 从未启动会抛 ServiceNotStartedException(被包内 catch)→ 停止后返回空路径,但**文件仍正常保存到 Movies/ScreenRecordings**
  - 插件 build.gradle:compileSdk 35 / Kotlin 2.1.0 / jvmTarget 17,与项目工具链兼容
  - 依赖 HBRecorder(jitpack)+ appcompat 1.6.0,CI 需访问 jitpack.io
- flutter_foreground_task 9.1.0 依赖干净(plugin_platform_interface ^2.1.8 / shared_preferences ^2.5.3 / platform ^3.1.6),其 manifest 需 app 手动声明 ForegroundService(但 2.0.25 从不启动它,可不声明)
- media_kit 1.2.2 依赖 web ^1.1.0 不依赖 js 包 → 与 flutter_screen_recording_web 的 js ^0.7.1 无冲突 → CI pub get 可成功

### 修复(已改工作区,未提交)
- `screen_recorder_service.dart`:
  - `startRecordScreenWithInternalAudio` → `startRecordScreenAndAudio`(麦克风音,可录到外放直播声)
  - 删除 `_isAndroid10Plus()` 与 device_info_plus import
  - **新增麦克风运行时权限申请**(Permission.microphone,Android 6+ 必需)
  - stop() 处理 2.0.25 返回空路径的情况:Toast 提示"已保存至系统相册(Movies/ScreenRecordings)"
- 用户需 git add/commit/push 触发 CI 重新构建
- 限制:2.0.25 只能录麦克风音(环境音),无法录直播内音;如需内音需自写 MediaProjection 插件(待用户决定)

## 下午 16:07:用户反馈三个功能都不工作+搜索变差，全面修复

### 问题根因
- **缩放**: 外层 zoom GestureDetector(在 live_room_page) 与内层 controls 的 Positioned.fill GestureDetector(在 player_controls) 存在手势竞技场冲突，ScaleGestureRecognizer 和 TapGestureRecognizer 竞争导致缩放无效
- **搜索**: search_controller 中 _tryOpenDouyinLink 的 _tryOpenByDouyinId 拦截逻辑可能导致搜索流程异常
- **录屏**: UI 接线已验证完整，无问题

### 修复内容(commit 14f9c28, 未推送)

1. **缩放修复** (live_room_page.dart + player_controls.dart):
   - 移除 live_room_page 中外层 zoom GestureDetector(保留 Transform+Obx 用于变换)
   - 将 onScaleStart/Update/End 移入 player_controls 全屏 Positioned.fill GestureDetector
   - 缩放手势现在由 controls 层统一处理(同一个 GestureDetector 同时处理 tap/double-tap/scale)，彻底消除冲突

2. **搜索修复** (search_controller.dart):
   - 恢复到 27e3ad5(直播id搜索)的稳定版本
   - 移除 _tryOpenByDouyinId 拦截和 simple_live_core/log 导入
   - 抖音号搜索通过 douyin_site.dart 增强的 searchRooms 实现(结果排序+fallback)，不需要 search_controller 拦截

3. **录屏**: 确认 UI 接线完整(直播页"更多"菜单 + 全屏controls + main.dart 注册 + AndroidManifest + ProGuard)

---

## 下午 16:26: 用户澄清录屏 → 录制直播流

用户实际需要的是录制直播流（抓取抖音 API 返回的 FLV 数据流），不是屏幕录制。

### 实现内容(未提交, E盘 git index 写入失败)

1. **新建 LiveStreamRecorderService** (`live_stream_recorder_service.dart`):
   - 使用 dio ResponseType.stream 直接下载 FLV 流到本地文件
   - 从 LiveRoomController 取最高画质 URL: `qualites[0].data[0]` = FLV 地址
   - 同时传递 playHeaders（抖音可能需要 Referer/Cookie）
   - 文件保存至 `getApplicationDocumentsDirectory()/Recordings/` 目录
   - 文件名格式: `{房间标题}_{timestamp}.flv`
   - CancelToken 支持随时停止下载

2. **替换引用**:
   - `main.dart`: ScreenRecorderService → LiveStreamRecorderService
   - `live_room_page.dart`: "录屏" → "录制直播"，所有引用更新
   - `player_controls.dart`: 全屏录制按钮引用更新

3. **清理**:
   - 删除 `screen_recorder_service.dart`
   - `pubspec.yaml`: 移除 flutter_screen_recording 依赖
   - `AndroidManifest.xml`: 移除 RECORD_AUDIO/FOREGROUND_SERVICE 等录屏权限
   - `proguard-rules.pro`: 移除 screen_recording/foreground_task keep 规则
   - `permission_handler` 保留（utils.dart 图片/存储权限仍需要）

### 技术细节
- Douyin stream URLs: qualites[i].data = [flv_url, hls_url]，取 data[0] = FLV
- FLV 是单个连续流，比 HLS（分段m3u8+ts）更适合直接下载
- 不需要任何系统权限（不需要录屏/录音/通知权限）
- dio 已存在于项目中（^5.9.0）
